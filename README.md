# LAB5_B
This project is a simple User Management System developed using PHP and MySQL. The system allows users to register, login, update user information, and delete users. It also includes a dashboard to display all registered users. The project uses HTML and CSS for the interface and XAMPP as the local server environment.
